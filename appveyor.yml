version: 8.0.{build}
image: Visual Studio 2022
configuration: Release
build_script:
  - ps: dotnet build -c Release -p:ContinuousIntegrationBuild=True
test_script:
  - ps: dotnet test -c Release --no-build --no-restore
